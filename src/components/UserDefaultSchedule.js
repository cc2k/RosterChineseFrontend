import React from 'react';

export default function UserDefaultSchedule({ user, defaultSchedule, onChange }) {
  // Placeholder for UI to set a user's default recurring schedule (e.g., Mon-Fri, 9-5)
  return (
    <div>
      <h4>Default Weekly Schedule</h4>
      {/* Implement UI for selecting recurring days/times */}
      <p>Coming soon: Set a default weekly schedule for this user.</p>
    </div>
  );
}
